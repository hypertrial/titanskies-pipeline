select
    granule_id,
    concept_id,
    acquisition_start,
    acquisition_end,
    cmr_revision_at,
    last_seen_at,
    download_url,
    local_path,
    checksum_sha256,
    file_size_bytes,
    observation_time,
    observation_hour,
    discovery_status,
    download_status,
    validation_status,
    processing_status,
    discovered_at,
    downloaded_at,
    validated_at,
    processed_at,
    error_message,
    updated_at
from {{ source('tempo_no2_ops', 'granule_inventory') }}
