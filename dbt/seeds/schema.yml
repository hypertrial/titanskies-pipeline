version: 2

seeds:
  - name: tempo_no2_contract
    columns:
      - name: contract_key
        tests: [not_null, unique]
      - name: min_region_coverage
        tests: [not_null]
      - name: accepted_quality_flags
        data_type: varchar
  - name: tempo_no2_std_contract
    columns:
      - name: contract_key
        tests: [not_null, unique]
      - name: min_region_coverage
        tests: [not_null]
      - name: accepted_quality_flags
        data_type: varchar
  - name: riverpulse_events_contract
    columns:
      - name: contract_key
        tests: [not_null, unique]
      - name: contract_version
        tests: [not_null]
      - name: field_contract_version
        tests: [not_null]
      - name: collection_name
        tests: [not_null]
      - name: sword_version
        tests: [not_null]
